# Manage-ToDos
A simple Todo app built with React, allowing users to add, update, delete, and toggle tasks. Utilizes localStorage to persist todos across sessions. Features include task management (add, edit, delete, mark as complete), responsive UI, and usage of React hooks and context API for state management.
